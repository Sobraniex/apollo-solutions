name = "hermes-download-redirect"
main = "index.ts"
compatibility_date = "2024-09-01"

# Bindings (set via `wrangler kv:namespace create` and `wrangler secret put`):
#   - HERMES_RELEASE_KV: KV namespace ID
#   - GITHUB_RELEASES_TOKEN: fine-grained PAT (contents:read on hermes-dental-clone)
#   - GITHUB_REPO: e.g. "Sobraniex/hermes-dental-clone"

# [vars]
# GITHUB_REPO = "Sobraniex/hermes-dental-clone"

# [[kv_namespaces]]
# binding = "HERMES_RELEASE_KV"
# id = "<your-kv-namespace-id>"
